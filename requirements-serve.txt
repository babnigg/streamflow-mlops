# Inference dependencies only - no optuna, prefect, jupyter or matplotlib.
# Fully pinned, and pinned to the SAME versions as requirements.txt: the model
# is pickled by the training environment and unpickled here, so a version gap
# across that boundary is silent train/serve skew.
# xgboost-cpu:   same version as training; the default wheel bundles ~400MB of
#                CUDA we never use.
# mlflow-skinny: tracking client without the server/UI stack.
# scikit-learn:  the model is an xgboost.sklearn.XGBRegressor, so the wrapper
#                must be importable to deserialize it.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pandas==3.0.5
pyarrow==25.0.0
numpy==2.5.1
pyyaml==6.0.3
mlflow-skinny==3.15.1
xgboost-cpu==3.4.0
scikit-learn==1.9.0
