# python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/

# data - tracked by dvc, not git (the .dvc pointer files ARE committed)
/data/raw/*.parquet
/data/processed/
/data/features/*.parquet
/data/monitoring/*.parquet
/reports/*.html

# mlflow / later stages
mlruns/
*.db

# secrets - .env.example is the tracked template
.env

# os / editor
.DS_Store
Thumbs.db
.vscode/

# per-machine claude code settings (permission allow-list) - the shared
# .claude/settings.json, if one is ever added, stays tracked
.claude/settings.local.json
