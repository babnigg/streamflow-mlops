# core pipeline
requests
pandas
pyarrow
pyyaml
numpy
dvc

# modeling + tracking
mlflow==3.13.0
xgboost==3.3.0
scikit-learn==1.8.0
optuna==4.9.0
optuna-integration[mlflow]==4.9.0

# serving (stage 3)
fastapi
uvicorn[standard]
pydantic

# monitoring (stage 4) - heavy: pulls a web-server stack,
# so deliberately NOT in requirements-serve.txt
evidently==0.7.21

# orchestration
prefect

# dev / notebooks (not needed in the container - see .dockerignore)
matplotlib
jupyter
pytest==9.1.1
