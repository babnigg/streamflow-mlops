# stage 1
requests
pandas
pyarrow
pyyaml
matplotlib
numpy
jupyter
dvc
mlflow==3.13.0
xgboost==3.3.0
scikit-learn==1.8.0
optuna==4.9.0
optuna-integration[mlflow]==4.9.0
pytest==9.1.1

# later stages:
# scikit-learn, lightgbm      # 2: models
# mlflow                      # 2: tracking + registry
# fastapi, uvicorn            # 3: serving
# evidently                   # 4: monitoring
prefect                     # orchestration (or apache-airflow)
